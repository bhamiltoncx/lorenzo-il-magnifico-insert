# The upstream .scad files use CRLF. Normalize to LF in the repository so that
# diffs stay reviewable regardless of a contributor's core.autocrlf setting.
*.scad text
*.md   text
*.sh   text eol=lf

# Never apply line-ending normalization to font binaries.
*.ttf binary
